package gutta.prediction.domain;

/**
 * A {@link UseCase} represents a business-related use case of the software to be analyzed.
 * Thus, it provides a connection between the code and the business domain.
 * 
 * @param name The use case's name
 */
public record UseCase(String name) {

}
